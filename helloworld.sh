#!/bin/bash    

# the above line is not a comment, it is called shebang.
# all that the shebang does is to instruct the os to run the script with /bin/bash executable.

echo "hello world" # echo is used to print something on screen, similar to what echo does in linux command line.
